import { NuiDragDirective } from './nui-drag.directive';

describe('NuiDragDirective', () => {
  it('should create an instance', () => {
    const directive = new NuiDragDirective();
    expect(directive).toBeTruthy();
  });
});
